import React from 'react';
import styled from '@emotion/styled';

const Container = styled.div`
    width: 100vw;
    height: 100vh;
`;

const Main: React.FC = () => {
    return (
        <Container>메인</Container>
    );
}

export default Main;